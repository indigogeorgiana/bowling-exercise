const scoreSpareFrame = require('../game.js')
test('scoreSpareFrame adds frame[0] and frame[1] and returns total', function () {
// Arrange
  const expected = [ ]

  // Act
  const actual = scoreSpareFrame.add(frames)

  // Assert

  expect(actual).toEqual(expected)
})
